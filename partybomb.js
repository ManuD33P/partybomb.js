include('files.js');
include('command.js');
function onLoad(){
 var temp = openFile();
 if(temp){
    // mandar mensaje que se realizo la operación
 } else{
    // mandar mensaje que fallo la operación.
 }
 // mandar mensajes que tenga que ver con el script.
 print('load partybomb.js');
 print('script partybomb.js by Manu16');
 
}
